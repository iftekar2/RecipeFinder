import styled from "styled-components";
import MenuComponent from "../Components/MenuComponent";

function Menu() {
  return (
    <div>
      <MenuComponent />
    </div>
  );
}

export default Menu;
